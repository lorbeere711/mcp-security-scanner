{
  "name": "strict-safe-server",
  "license": "MIT",
  "permissions": ["filesystem:read"],
  "allowedPaths": ["/workspace"],
  "allowedHosts": ["docs.example.com"],
  "tools": [
    {
      "name": "search_docs",
      "description": "Search local docs using validated query filters."
    }
  ]
}
