{
  "name": "safe-filesystem-server",
  "license": "MIT",
  "permissions": ["filesystem:read"],
  "allowedPaths": ["/workspace"],
  "allowedHosts": ["docs.example.com"],
  "tools": [
    {
      "name": "search-index",
      "description": "Search internal index with strict schema validation"
    }
  ]
}
